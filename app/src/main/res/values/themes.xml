<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GithubUser" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.GithubUser" parent="Base.Theme.GithubUser" />

    <style name="SplashStyle" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/github</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:windowBackground">@color/github</item>
        <!--&lt;!&ndash;        <item name="windowSplashScreenBackground">@color/merah_tua</item>&ndash;&gt; ini kenapa gak bisa ya kak mala widowedBackground yang bisa-->
        <item name="android:animationDuration">2000</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/github</item>
    </style>
</resources>